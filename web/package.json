{
  "name": "tirai-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Tirai confidential RFQ desk — static UI + JSON Ledger API proxy (Node stdlib, no deps)",
  "scripts": {
    "start": "node server.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
